import type { ActivityEvent, IntegrationAction, IntegrationCategory, IntegrationKey, IntegrationResult, IntegrationStatus, IntegrationTaskInput, PermissionDecision, PermissionLevel } from "@/types";
export interface IntegrationAdapter { id: IntegrationKey; name: string; category: IntegrationCategory; connect: () => Promise<void>; disconnect: () => Promise<void>; getStatus: () => Promise<IntegrationStatus>; sync: () => Promise<ActivityEvent[]>; getActivity: () => Promise<ActivityEvent[]>; createTask?: (input: IntegrationTaskInput) => Promise<IntegrationResult>; executeAction: (action: IntegrationAction) => Promise<IntegrationResult>; validatePermissions: (action: IntegrationAction, permission: PermissionLevel) => Promise<PermissionDecision>; }
