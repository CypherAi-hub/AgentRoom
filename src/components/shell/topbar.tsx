"use client";
import { Menu, Search } from "lucide-react";
import { Button } from "@/components/ui/primitives";
import { mockApprovals, mockIntegrations } from "@/lib/mock-data";
export function Topbar({ onMenu, onCommand }: { onMenu: () => void; onCommand: () => void }) { const connected = mockIntegrations.filter((x) => x.status === "connected").length; const pending = mockApprovals.filter((x) => x.status === "pending").length; return <header className="sticky top-0 z-30 flex h-16 min-w-0 items-center justify-between gap-3 overflow-hidden border-b bg-background/80 px-4 backdrop-blur xl:px-6"><div className="flex min-w-0 items-center gap-3"><Button variant="ghost" size="icon" className="shrink-0 lg:hidden" onClick={onMenu} aria-label="Open navigation"><Menu className="size-4" /></Button><div className="min-w-0"><div className="truncate text-sm font-semibold">FoFit Operating Room</div><div className="truncate text-xs text-muted-foreground">Mock-first command center / approval-safe</div></div></div><div className="flex shrink-0 items-center gap-2"><div className="hidden items-center gap-2 rounded-full border px-3 py-1.5 text-xs text-muted-foreground md:flex"><span className="size-2 rounded-full bg-emerald-300" />{connected} connected tools</div><div className="hidden rounded-full border px-3 py-1.5 text-xs text-muted-foreground md:block">{pending} approvals pending</div><Button variant="outline" size="sm" className="px-2.5 sm:px-3" onClick={onCommand} aria-label="Open command bar"><Search className="size-4" /><span className="hidden sm:inline">Command</span></Button></div></header>; }
