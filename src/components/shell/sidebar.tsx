"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Activity, Bot, CheckSquare, Gauge, GitBranch, Home, PlugZap, Settings, Workflow } from "lucide-react";
import { cn } from "@/lib/utils";
const nav = [{ href: "/dashboard", label: "Dashboard", icon: Home }, { href: "/rooms", label: "Rooms", icon: Gauge }, { href: "/agents", label: "Agents", icon: Bot }, { href: "/integrations", label: "Integrations", icon: PlugZap }, { href: "/settings", label: "Settings", icon: Settings }];
const room = [{ href: "/rooms/fofit", label: "Overview", icon: Gauge }, { href: "/rooms/fofit/tasks", label: "Tasks", icon: CheckSquare }, { href: "/rooms/fofit/activity", label: "Activity", icon: Activity }, { href: "/rooms/fofit/workflows", label: "Workflows", icon: Workflow }, { href: "/rooms/fofit/approvals", label: "Approvals", icon: GitBranch }];
export function Sidebar({ onNavigate }: { onNavigate?: () => void }) { const pathname = usePathname(); const item = (x: typeof nav[number]) => { const active = pathname === x.href || (x.href !== "/dashboard" && pathname.startsWith(x.href)); return <Link key={x.href} href={x.href} onClick={onNavigate} className={cn("flex items-center gap-3 rounded-md px-3 py-2 text-sm text-muted-foreground transition-colors hover:bg-secondary hover:text-foreground", active && "bg-secondary text-foreground")}><x.icon className="size-4" />{x.label}</Link>; }; return <aside className="flex h-full flex-col border-r bg-background/80 p-4 backdrop-blur"><Link href="/dashboard" onClick={onNavigate} className="mb-6 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-lg border bg-secondary font-mono text-sm font-black">AR</div><div><div className="text-sm font-semibold">Agent Room</div><div className="text-xs text-muted-foreground">AI operations room</div></div></Link><nav className="flex flex-col gap-1">{nav.map(item)}</nav><div className="mt-8"><div className="mb-2 px-3 text-xs font-medium uppercase tracking-[0.16em] text-muted-foreground">FoFit Room</div><nav className="flex flex-col gap-1">{room.map(item)}</nav></div><div className="mt-auto rounded-lg border bg-card/70 p-3"><div className="text-xs font-medium uppercase tracking-[0.16em] text-muted-foreground">Mock-first</div><p className="mt-2 text-sm leading-5 text-muted-foreground">No real API keys, auth, billing, or database writes are active.</p></div></aside>; }
