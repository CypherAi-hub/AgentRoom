import { TaskBoard } from "@/components/tasks";
import { RoomNotFound } from "@/components/rooms/room-not-found";
import { getRoomBySlug, getRoomTasks } from "@/lib/mock-data";
export default async function RoomTasksPage({ params }: { params: Promise<{ roomId: string }> }) { const { roomId } = await params; const room = getRoomBySlug(roomId); if (!room) return <RoomNotFound />; return <div className="flex flex-col gap-6"><div><p className="text-sm font-medium uppercase tracking-[0.18em] text-muted-foreground">{room.name}</p><h1 className="mt-2 text-3xl font-semibold">Task board</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">Mock kanban board for assigning work to agents and linking work to tools. Drag-and-drop is intentionally out of scope for v1.</p></div><TaskBoard tasks={getRoomTasks(room.id)} /></div>; }
